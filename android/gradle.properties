org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# AGP 9 compiles Kotlin itself, so a plugin no longer has to bring the Kotlin
# Gradle Plugin with it. The Flutter template ships this off; it is on here so
# the patched flutter_tts in third_party/ compiles without applying KGP.
android.builtInKotlin=true
